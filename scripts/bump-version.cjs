const fs=require('fs'),path=require('path');const ROOT=path.join('D:','OneDrive','SISTEMA BROTAR','assistente-t\u00e9cnico-de-nutri\u00e7\u00e3o-escolar');const type=process.argv[2]||'patch';const desc=process.argv[3]||'Atualizacao';const pkg=JSON.parse(fs.readFileSync(path.join(ROOT,'package.json'),'utf8'));const [major,minor,patch2]=pkg.version.split('.').map(Number);let v;if(type==='major')v=`${major+1}.0.0`;else if(type==='minor')v=`${major}.${minor+1}.0`;else v=`${major}.${minor}.${patch2+1}`;const today=new Date().toISOString().split('T')[0];const oldV=pkg.version;pkg.version=v;fs.writeFileSync(path.join(ROOT,'package.json'),JSON.stringify(pkg,null,2)+'\n','utf8');let ct=fs.readFileSync(path.join(ROOT,'constants.ts'),'utf8');ct=ct.replace(`APP_VERSION = '${oldV}'`,`APP_VERSION = '${v}'`);ct=ct.replace(/RELEASE_DATE = '\d{4}-\d{2}-\d{2}'/,`RELEASE_DATE = '${today}'`);const entry=`  {\n    version: '${v}',\n    date: '${today}',\n    description: '${desc}',\n    changes: ['${desc}.']\n  },\n`;ct=ct.replace('export const RELEASE_NOTES = [\n',`export const RELEASE_NOTES = [\n${entry}`);fs.writeFileSync(path.join(ROOT,'constants.ts'),ct,'utf8');const clPath=path.join(ROOT,'CHANGELOG.md');let cl=fs.existsSync(clPath)?fs.readFileSync(clPath,'utf8'):'# Changelog\n\n';const clE=`## [${v}] - ${today}\n\n### ${type==='patch'?'Fixed':type==='minor'?'Added':'Changed'}\n- ${desc}\n\n---\n\n`;cl=cl.replace('# Changelog\n\n',`# Changelog\n\n${clE}`);fs.writeFileSync(clPath,cl,'utf8');console.log(`OK: ${oldV} -> ${v}`);